-  name: install packages
   hosts: local
   connection: local
   tasks: 
   - name: install packages
     ansible.builtin.debug:
      name: "{{ item }}" 
      state: present
     loop: 
     - mysql
     - nginx
     - zip 
